input {
  redis {
    host => "redis"
    port => 6379
    type => "redis-input"
    data_type => "list"
    key => "logstash"
    codec => "json"
  }
}

output {
#  elasticsearch {
#    embedded => false
#    host => "elasticsearch"
#    port => 9300
#  }

  file {
    path => "/config-dir/logstash.out"
  }
}
